<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Admin Interface</title>
    <link rel="stylesheet" href="{{ url_for('static', filename='styles.css') }}">
</head>
<body>
    <h1>Admin Interface</h1>
    <h2>Reservation Queue</h2>
    <ul>
        {% for reservation in reservation_queue %}
            <li>Name: {{ reservation.name }}, Party Size: {{ reservation.party_size }}</li>
        {% endfor %}
    </ul>
    <h2>Reserved Tables</h2>
    <ul>
        {% for table in reserved_tables %}
            <li>Name: {{ table.name }}, Party Size: {{ table.party_size }}</li>
        {% endfor %}
    </ul>
    <form action="{{ url_for('process_reservation') }}" method="get">
        <button type="submit">Process Next Reservation</button>
    </form>
    <form action="{{ url_for('clear_reservations') }}" method="get">
        <button type="submit">Clear Reservations</button>
    </form>
    <form action="{{ url_for('logout') }}" method="get">
        <button type="submit">Logout</button>
    </form>
</body>
</html>
